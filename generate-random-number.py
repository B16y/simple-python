import random   # import the random module

num = random.randint(1,10)  # here it will start from 1 and end at 10 unlike range that it would have ended at 9 only but here in randint we will end at 10.
print(num)